language: java

jdk:
- oraclejdk7

env:
- PLAY_VERSION=1.2.4

before_script:
- wget http://downloads.typesafe.com/play/${PLAY_VERSION}/play-${PLAY_VERSION}.zip
- unzip -q play-${PLAY_VERSION}.zip

script:
- export FAPSDK=$WORKSPACE
- echo $FAPSDK
- cd fap
- ant -f compiler.xml